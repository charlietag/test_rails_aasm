<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :aasm_state %>

    <% #book.aasm.events(permitted: true).map(&:name).each do |event| %>
      <% #link_to event, book, method: :patch, class: "btn btn-default" %>
    <% #end %>
    <%# book.aasm.events(permitted: true).each do |event| %>
      <%# link_to event.name, book, method: :patch, class: "btn btn-default" %>
    <%# end %>

    <%# form.select(:aasm_state, book.aasm.events(permitted: true).map(&:name)) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
